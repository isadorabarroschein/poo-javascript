class Pessoa {
    constructor(nome, idade, altura, peso) { // Atributo
     // Iniciando dos atributos
     this.nome = nome;
     this.idade = idade;
     this.altura = altura;
     this.peso = peso;
    }
}